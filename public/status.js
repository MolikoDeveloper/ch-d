const $=q=>document.querySelector(q);
const fmt=new Intl.NumberFormat('es-CL');
const compact=new Intl.NumberFormat('es-CL',{notation:'compact',maximumFractionDigits:1});
function el(tag,text,cls){const n=document.createElement(tag);if(cls)n.className=cls;if(text!=null)n.textContent=String(text);return n}
function formatDate(v){return v?new Date(v).toLocaleString('es-CL',{dateStyle:'medium',timeStyle:'short'}):'—'}
function renderConnectorProgress(items){const box=$('#connectorProgress');box.replaceChildren();for(const x of items||[]){const current=Number(x.current)||0,total=x.total==null?null:Number(x.total)||0,pct=total?Math.min(100,current/total*100):null,row=el('div',null,'connector-row'),head=el('div',null,'connector-head'),left=el('div');left.append(el('b',x.name),el('small',x.run?.message||`${fmt.format(current)} ${x.unit}`));head.append(left,el('span',total?`${fmt.format(current)} / ${fmt.format(total)}`:fmt.format(current)));row.append(head);if(total){const track=el('div',null,'connector-track'),fill=el('i',null,'connector-fill');fill.style.width=`${pct}%`;track.appendChild(fill);row.append(track)}box.appendChild(row)}if(!box.children.length)box.appendChild(el('div','Sin conectores registrados.','empty-state'))}
function renderRuns(runs){const box=$('#recentRuns');box.replaceChildren();if(!runs?.length){box.appendChild(el('div','Sin actividad reciente.','empty-state'));return}for(const r of runs){const row=el('div',null,'activity-row'),info=el('div'),detail=[r.status,r.message,formatDate(r.finished_at||r.started_at)].filter(Boolean).join(' · ');info.append(el('b',r.source_id),el('small',detail));row.append(info,el('span',compact.format(Number(r.records_written)||0)));box.appendChild(row)}}
async function load(){if(document.hidden)return;const p=await fetch('/api/progress').then(r=>r.json());$('#statusLabel').textContent=p.activeRuns?.length?`Sincronizando ${p.activeRuns.map(r=>r.source_id).join(', ')}`:'Sin sincronizaciones activas';$('#statusTime').textContent=formatDate(p.generatedAt);renderConnectorProgress(p.connectorProgress||[]);renderRuns(p.recentRuns||[])}
let timer=null;function start(){if(timer)clearInterval(timer);timer=setInterval(()=>load().catch(console.error),5000)}
document.addEventListener('visibilitychange',()=>{if(document.hidden){if(timer)clearInterval(timer);timer=null}else{load().catch(console.error);start()}});
load().then(start).catch(console.error);
